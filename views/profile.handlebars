{{#if user}}
    <div>You did the thing and logged in with twitter!</div>
    <div>{{user.username}}</div>
    <img src="{{user._json.profile_image_url}}">
    <div>{{user.displayName}}</div>
    <div>{{user._json.created_at}}</div>


{{else}}
    <div> you fucked up</div>
{{/if}}